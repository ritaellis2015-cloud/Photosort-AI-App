# Optional: enables real AI-generated captions via Claude's vision API.
# Without this, the Caption Generator falls back to template captions.
ANTHROPIC_API_KEY=

# Optional: override the Claude model used for captions (defaults to claude-sonnet-5).
ANTHROPIC_MODEL=claude-sonnet-5
